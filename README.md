Minimal proof of concept of a monaco editor that:

- allows dropping files
- allows pasting context (becomes a `/paste/` url)
- allows seeing how many tokens any url contains
- allows suggestions when doing `@`
